#fichier de paramètres  pour le package servo
#objectif : lister les différents appareils branchés à la plaque Maestro
#enventuellement des bornes physiques de limitation
#et un nom standard pour proposer une implémentation indépendante des branchements pour les autres modules

#lien vers le fichier /dev de la carte Maestro


devices: ["motor", "vitesse", "direction", "servo1", "servo2", "servo3"]

motor: {
    port: 0,
    min: 4000,
    max: 8000,
    cmin: -100,
    cmax: 100
}

vitesse: {
    port: 5,
    min: 4600,
    max: 6100,
    cmin: 1,
    cmax: 3
}

direction: {
    port: 6,
    min: 4900,
    max: 7100,
    cmin: -100,
    cmax: 100
}

servo1: {
    port: 0,
    min: 4000,
    max: 8000,
    cmin: 4000,
    cmax: 8000
}

servo2: {
    port: 1,
    min: 4000,
    max: 8000,
    cmin: 4000,
    cmax: 8000
}

servo3: {
    port: 9,
    min: 4000,
    max: 8000,
    cmin: 4000,
    cmax: 8000
}
